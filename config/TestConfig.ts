/**
 * TestConfig = Environment layer
 * base URL
 * timeouts
 * browser settings
 * env-specific values
 */

export class TestConfig {
    public static readonly BASE_URL = "https://tutorialsninja.com/demo/";
}

